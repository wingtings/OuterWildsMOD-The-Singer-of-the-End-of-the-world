# 2026.5.26 开发日志 — 地图美化、剧情接线与结局判定

> Author: wingtings
>
> 本轮目标：补全叙事缺口，让 singer_world 不再是"空无一物的荒凉星球"，把已写好但游离的文本/对话接入游戏，
> 修正恒星与轨道参数，放置角色占位模型，写完结局判定并接入主 cs。对照阅读 `logs/design.md`（剧本）、`logs/5.21 Phase3.md`（技术模板）。

---

## 〇、本轮成果总览

- ✅ 修正恒星亮度过大、两颗星球离恒星过近的问题
- ✅ 世末之城从 2 栋建筑扩充到 8 栋（广场 / 音乐厅 / 高塔三个分区），不再荒凉
- ✅ 用挪麦人雕像作为歌者、天依的**占位模型**（等 MMD 资产打包好再替换）
- ✅ 把 `singer_diary.xml`（歌者日记）、`bet_stele.xml`（赌约石碑）通过 `translatorText` 真正接入游戏
- ✅ 给 Ship Log 加上 `revealVolumes` + signal `reveals` + 文本 `ShipLogConditions`，让 8 个日志节点真能解锁
- ✅ 补全天依对话（新增"修复扩音器后"分支），新建扩音装置修复交互
- ✅ 写完 `EndingJudge.cs` 结局判定并在主 cs 中挂载
- ✅ `dotnet build` 通过（0 警告 0 错误），所有 JSON/XML 通过语法校验

> 现在的状态：玩家可以**从着陆 → 探索三区 → 读日记/石碑 → 修复扩音装置 → 触发 True End** 完整跑通一条叙事主线。
> 角色还是占位雕像、雨还是引擎自带、True End 还是"通知 + 日志"的最小演出——这些留到后续阶段。

---

## 一、恒星与轨道修正

**问题**：恒星亮度过大；神谕之境、世末之城离恒星太近。

**改动**（`planets/outing_sun.json`）：

| 参数 | 原值 | 新值 | 说明 |
|------|------|------|------|
| `solarLuminosity` | 3 | **1.0** | 光照强度降到原来的 1/3，这是亮度的主控杆 |
| `Star.size` | 2000 | **1500** | 缩小恒星视觉半径，避免占满天空 |
| `Base.surfaceSize` | 2000 | **1500** | 与 size 同步 |
| `lightRadius` | 50000 | 60000 | 光照范围略增，保证推远后的星球仍被照到 |

**轨道推远**：

| 星球 | `semiMajorAxis` 原值 | 新值 | 距恒星表面间距（半径 1500）|
|------|------|------|------|
| 神谕之境 (`god_realm.json`) | 3000 | **7000** | 约 5500（原来仅 1000，几乎贴着恒星）|
| 世末之城 (`singer_world.json`) | 8000 | **15000** | 约 13500 |

> 神谕之境仍比世末之城更靠近恒星，符合"靠近恒星运行的小星球"的设定。
> 世末之城本身是"末日昏暗"基调（`atmosphereSunIntensity:0.6` + `ambientLight:1.2`），即使恒星变暗也不会全黑。

---

## 二、世末之城城市布景（不再荒凉）

`planets/singer_world.json` 的 `Props.details` 从 2 栋扩充到 **8 栋**黑荆棘悬挂城市建筑，围绕出生点（10,302,0）布置成一座小城，并划分三个功能区：

| 区域 | 位置(约) | 内容 |
|------|---------|------|
| 雨中广场 | 中心 (0,~290,0) | 主建筑 + 日记墙 + 天依占位 + 信号探测起点 |
| 废弃音乐厅 | 南侧 (0,~284,-65) | 大型建筑(scale 1.0) + 歌者占位 + 扩音装置交互 |
| 高塔观测台 | 边缘 (85,~282,60) | 高建筑 + 高塔日志触发体 |

- `scatter` 间歇泉数量 3→4，作为城市中的视觉点缀。
- 建筑全部 `alignRadial:true` 贴合球面。

### 角色占位模型（重要：这是占位符）

按你的要求，用**挪麦人雕像**作为歌者和天依的占位，等后续 MMD 资产打包好再替换：

```json
{ "path": "TimberHearth_Body/Sector_TH/Sector_Village/Sector_Observatory/Interactables_Observatory/Prefab_NOM_Statue",
  "rename": "蓝发女孩(天依占位)", "position": { "x": 20, "y": 301, "z": 10 }, ... }
```

- 天依占位在广场 (20,301,10)，与对话触发体重合。
- 歌者占位在音乐厅舞台 (0,301,-58)。

> ⚠ **占位路径需在游戏内核实**：这条挪麦人雕像路径是我的最佳推测。进游戏后若雕像没出现，按 F7 打开 Unity Explorer 搜索 `Statue` / `Solanum` 找到正确路径替换即可——这只是一个字符串的事，不影响其它布景。
>
> **备选占位路径**（任选其一）：
> - 索拉努姆（量子月上站立的挪麦人，最像"人"）——但量子月是条件加载，可能不稳定
> - 海尔digo 旅行者（Esker/Gabbro 等 Hearthian，会演奏乐器，主题上很贴"歌者"，但不是挪麦人）
> - 各星球的挪麦人雕像（余烬星 `CaveTwin_Body` 的无光之城里有多座）

---

## 三、文本接线与 Ship Log 解锁

**之前的问题**：`singer_diary.xml`、`bet_stele.xml` 内容写得很好，但**没有任何星球 JSON 引用它们**，游戏里根本看不到；而且 Ship Log 的探索事实几乎没有任何解锁触发器。

### 3.1 用 translatorText 接入文本

- `singer_world.json` → 广场墙壁放歌者日记（`type:"wall"`, `seed:42`）。
- `god_realm.json` → 放赌约石碑（`type:"wall"`, `seed:7`）+ 一个"录音石"占位 detail。

### 3.2 文本读取解锁日志（ShipLogConditions）

- `singer_diary.xml`：读到第 1 段 → 解锁 `SINGER_DIARY_FACT`。
- `bet_stele.xml`：读完 1,2 段 → `STELE_EXPLORE`；读完 1,2,3 段 → `THIRD_PARTY_FACT` + `THIRD_PARTY_LINK`。

### 3.3 revealVolumes（走到即解锁）

**世末之城**（`Volumes.revealVolumes`）：
- 着陆/广场区(r=25)：`SINGER_CITY_EXPLORE` `SQUARE_EXPLORE` `SQUARE_RUMOR` `TOWER_RUMOR`
- 音乐厅(r=18)：`HALL_EXPLORE` `AMPLIFIER_FACT`
- 高塔(r=18)：`TOWER_EXPLORE` `TOWER_PART_FACT` `GOD_REALM_RUMOR`

**神谕之境**：
- 着陆区(r=40)：`GOD_REALM_EXPLORE` `STELE_RUMOR`
- 录音石(r=8)：`GOD_VOICE_FACT`

### 3.4 signal 识别解锁

歌者信号"歌者之声"加 `"reveals": "HALL_RUMOR"`，并把信号源从广场中心移到音乐厅舞台 (0,302,-58)，`detectionRadius` 800→1000，让玩家循着歌声找到音乐厅。

### 3.5 好奇心配色

`outing_system.json` 补 `curiosities` 定义 `OUTING_MYSTERY` 为暗紫色（呼应"鸥停之谜"）。

### 3.6 新增的两个 Fact

`singer_city_log.xml` 补 `AMPLIFIER_FIXED`（扩音器修复）和 `TRUE_ENDING_FACT`（结局），供对话和结局判定揭示。

---

## 四、对话与扩音器修复

### 4.1 天依对话（`tianyi_dialogue.xml`）

新增"修复后"分支 `AfterRepair`（`EntryCondition: AMPLIFIER_REPAIRED`），并**放在文件最前**——OW 对话按顺序取第一个满足 EntryCondition 的节点，DEFAULT 永远满足，所以条件分支必须排在 DEFAULT 之前。

- 未修复：呆望天空，"……下雨了。一直在下。"
- 已修复：听到歌声，迈出一步又停下，"我应该……过去看看吗？"（设 `TIANYI_HEARD_SONG`）

### 4.2 扩音装置交互（新建 `amplifier_dialogue.xml`）

放在音乐厅舞台后方 (3,301,-62)。交互 → 选择"嵌入零件修复" → 设置 `AMPLIFIER_REPAIRED`（本循环条件）+ `AMPLIFIER_EVER_REPAIRED`（持久）+ 揭示 `AMPLIFIER_FIXED`。已修复后再交互只显示运转提示。

> ⚠ **本轮简化**：原设计的"找到零件 A(广场)+零件 B(高塔)→插入插槽"完整 fetch-quest，本轮**简化为一次对话交互即可修复**。
> 原因：NH 的 `item`/`itemSocket` schema 我没有十足把握，写错可能导致整颗星球加载失败（反而让星球变荒凉）。
> 当前用纯对话系统实现，是我**有把握能正确加载**的方案，叙事主线照样跑通。
> 完整的零件拾取/插槽谜题列为后续增强（见第七节）。

---

## 五、结局判定（`EndingJudge.cs` + 主 cs 接入）

### 5.1 EndingJudge.cs

之前是空文件，现在实现为一个 MonoBehaviour：
- 每 0.5 秒轮询 `DialogueConditionManager.SharedInstance.GetConditionState("AMPLIFIER_REPAIRED")`。
- 条件为真时触发一次 `TriggerTrueEnding()`：
  - `Locator.GetShipLogManager().RevealFact("TRUE_ENDING_FACT")` 揭示结局日志
  - `NotificationManager` 弹出屏幕通知："雨停了。云层裂开，阳光穿透乌云。轮回终结，世界得赎。"
- 用了 OW 内置的稳定 API（DialogueConditionManager / Locator / NotificationManager）。

### 5.2 主 cs 接入

`TheSingerOfTheEnd.cs` 的 `OnStarSystemLoaded` 在确认是 `outing_system` 后，`new GameObject("SingerEndingJudge").AddComponent<EndingJudge>()`。该事件每次进入星系（每个循环）触发，判定器随场景重建。

> 检查的是**本循环条件**（非持久），所以结局在"修复扩音器的那个循环"当场触发——这正是设计里的高潮时刻。

---

## 六、当前可玩流程

```
着陆世末之城(暴雨/浓雾/闪电)
  → 进广场:解锁城市/广场日志,看到天依占位,读墙上日记
  → 信号探测器切"歌者频率",循歌声找到音乐厅,看到歌者占位 + 损坏的扩音装置
  → 登高塔:解锁观测台,看到远处的神谕之境
  → 飞神谕之境:读三块石碑,理解赌约 + 第三者条款,听录音石
  → 回音乐厅,交互扩音装置 → 修复(AMPLIFIER_REPAIRED=true)
  → EndingJudge 检测到 → True End 通知 + 结局日志
  → (可选)再找天依,对话变为"听到歌声"分支
```

---

## 七、已知事项 / 待办（下一步）

**进游戏后需核实**（这些是占位/推测，不影响编译，但要在游戏里看一眼）：
- [ ] 挪麦人雕像 `Prefab_NOM_Statue` 路径是否正确（见第二节备选路径）
- [ ] 8 栋建筑的 `HangingCity_BuildingIsland_v2 (N)` 索引是否都存在（部分索引若不存在，那栋不显示，其余正常）
- [ ] `translatorText` 的螺旋文字朝向/位置是否合适（调 `seed` 和 `rotation`）
- [ ] revealVolumes 半径是否覆盖到位（用 Collider Visualizer 看）
- [ ] JSON 里的 `"comment"` 字段若在控制台报 schema 警告，可删除（非致命）

**后续增强**（按 README 路线，图形学是评分核心，务必留足时间）：
- [ ] 扩音器完整 fetch-quest（`item` 拾取零件 A/B + `itemSocket` 插槽），替换当前的对话简化版
- [ ] 把修复 gate 在"已理解第三者条款"之后（需要把读石碑也设一个 condition）
- [ ] 角色 MMD 模型（Blender→FBX→Humanoid→Prefab→AssetBundle）替换占位雕像
- [ ] **图形学主线**：打通 AssetBundle 管线 → 体积雨/涟漪 shader → God Rays（用于 True End 雨停穿云 + 舞台追光）
- [ ] True End 完整演出：雨停、阳光穿透、循环停止（目前仅通知 + 日志）
- [ ] `TimelineManager.cs`：让雨量/颜色随 22 分钟渐变（灰→红）

---

## 八、本轮改动文件清单

| 文件 | 改动 |
|------|------|
| `planets/outing_sun.json` | 降亮度(solarLuminosity 3→1)、缩小恒星(2000→1500) |
| `planets/god_realm.json` | 轨道推远(3000→7000)、接入石碑文本、加录音石占位、加 revealVolumes |
| `planets/singer_world.json` | 轨道推远(8000→15000)、8 栋建筑、角色占位、移信号+reveals、日记墙、扩音器对话、revealVolumes |
| `systems/outing_system.json` | 加 OUTING_MYSTERY 好奇心配色 |
| `planets/text/singer_diary.xml` | 加 ShipLogConditions |
| `planets/text/bet_stele.xml` | 加 ShipLogConditions |
| `planets/shiplog/singer_city_log.xml` | 加 AMPLIFIER_FIXED、TRUE_ENDING_FACT 两个 Fact |
| `planets/dialogue/tianyi_dialogue.xml` | 加"修复后"分支 |
| `planets/dialogue/amplifier_dialogue.xml` | **新建**：扩音装置修复交互 |
| `EndingJudge.cs` | **从空文件实现**：结局判定 |
| `TheSingerOfTheEnd.cs` | OnStarSystemLoaded 挂载 EndingJudge |

---

# 5.26（二）测试反馈修复 — Ship Log 显示与资产路径

> 上半部分的改动进游戏测试后暴露两个问题，本节记录排查与修复。

## 一、问题现象

1. **飞船日志提示"已更新"，但打开后空无一物**。
2. **星球表面依旧荒凉**——说明 prop 资产路径完全无效，建筑/角色根本没加载出来。

## 二、Ship Log 为空的根因与修复

查 [NH Ship Log 文档](https://nh.outerwildsmods.com/guides/ship-log/) 后定位到两点：

- **`mapMode.manualPosition` 必须与 `manualNavigationPosition` 成对出现**，否则 map 模式直接不渲染。我之前只写了 `manualPosition`，导致 map 模式整页空白。文档原文：*不设 manualPosition 就走自动模式*。
- **rumor（谣言）模式的卡片排布依赖星系配置里的 `entryPositions`**，缺失时卡片堆在默认行、容易跑到屏幕外看不见。

**修复**：

| 文件 | 改动 |
|------|------|
| `singer_world.json` / `god_realm.json` | 删除 `ShipLog.mapMode.manualPosition` → 改用**自动 map 模式**（NH 按轨道自动摆放星球节点）|
| `outing_system.json` | 新增 `entryPositions`，为 8 个 Entry（SINGER_CITY / CITY_SQUARE / CITY_HALL / CITY_TOWER / GOD_REALM / BET_STELE / GOD_VOICE / THIRD_PARTY）指定 rumor 模式坐标 |

> 注：日志"能提示更新"说明 XML 本身是加载成功的（fact 存在才会触发揭示通知），所以这是纯**显示/排布**问题，不是 XML 没加载。

## 三、资产路径全部替换为验证过的基础游戏路径

读取社区维护的 [Props 列表（Google 表格）](https://docs.google.com/spreadsheets/d/1VJaglB1kRL0VqaXhvXepIeymo93zqhWex-j7_QDm6NE/edit?gid=0#gid=0)，逐条核对。结论：

- 之前用的 `HangingCity_BuildingIsland_v2 (N)`、`QuantumIsland .../Strucutre_NOM_Shelf`、`TimberHearth .../Prefab_NOM_Statue`、`Geyser_Village` **都不在验证列表里**，是无效/猜测路径——这就是荒凉的原因。
- ⚠ **重要**：表格里大量"房屋"类 prop（RingWorld / DreamWorld / DB 维度）属于 **DLC（Echoes of the Eye）** 资产。不能假设装了 DLC，因此本轮**只用基础游戏**（BrittleHollow / CaveTwin / TimberHearth / StatueIsland / Comet / QuantumMoon）的路径。

### 世末之城（`singer_world.json`）新布景（全部验证过）

| 用途 | prop | 来源 |
|------|------|------|
| 音乐厅 | 挪麦太阳塔（带巨窗+旋转门）`TLE_Exterior_Geo` | CaveTwin |
| 高塔 | 挪麦重力炮 `Structure_NOM_GravityCannon_HT` | CaveTwin |
| 广场废墟 ×3 | `Ruin_Empty` / `Ruin_Campfire` | BrittleHollow Crossroads |
| 发光门廊 | `Prefab_NOM_Doorway` | BrittleHollow |
| 立式火炬 ×2 | `Prefab_NOM_Torch` | CaveTwin（给昏暗雨城补光）|
| 长桌 / 书架 | `Props_NOM_Table` / `Prefab_NOM_Shelf (2)` | BrittleHollow BlackHoleForge |
| 散布·陶器碎片 ×18 | `Clutter_NOM_Pottery (1)` | StatueIsland |
| 散布·枯灌木 ×12 | `Prefab_Tree_BH_Bristle` | BrittleHollow |

### 角色占位改用"索拉努姆"（站立的挪麦人）

之前的挪麦雕像路径无效。改用社区验证过的**站立挪麦人**——索拉努姆（量子月上仰望天空的挪麦人）：

```
QuantumMoon_Body/Sector_QuantumMoon/State_EYE/Interactables_EYEState/ConversationPivot/Character_NOM_Solanum/Nomai_ANIM_SkyWatching_Idle
```

天依（广场 20,301,10）和歌者（音乐厅舞台 0,301,-56）各放一尊。索拉努姆"仰望天空"的姿态恰好契合两个角色（歌者望天而歌、天依麻木仰望积雨云）。后续用打包好的 MMD 模型替换。

> ⚠ 索拉努姆属于量子月 EYE 状态对象，极个别情况下可能加载异常。若进游戏没出现，备选：基础游戏的 Hearthian 旅行者（如 `Traveller_HEA_Gabbro`、`Villager_HEA_Esker` 等，会演奏乐器，主题上也贴"歌者"）。

## 四、神谕之境简单布景（`god_realm.json`）

修掉无效的 QuantumIsland 书架路径，改用验证过的挪麦 prop 装点赌约石碑区：

| 用途 | prop | 来源 |
|------|------|------|
| 录音石·神明之声 | 挪麦录音机 `Prefab_NOM_Recorder/Props_NOM_Recorder` | BrittleHollow 坠毁点 |
| 石碑前基座 | 挪麦路标 `Props_NOM_Trailmarker` | StatueIsland |
| 石碑两侧火炬 ×2 | `Prefab_NOM_Torch` | CaveTwin |
| 装点容器 | `Prefab_NOM_Container` | Comet |

## 五、验证

- `dotnet build`：0 警告 0 错误，已部署。
- `singer_world.json` / `god_realm.json` / `outing_system.json` 通过 JSON 语法校验。

## 六、本轮仍需进游戏核实

- [ ] 8 类建筑/角色 prop 是否都正确显示（现在路径来自验证表，命中率应大幅提高）
- [ ] 太阳塔、重力炮体积较大，`scale 0.5` 是否合适，位置 y 是否贴合地面（用 Debug Raycast 按 P 调）
- [ ] 索拉努姆占位是否成功加载（否则用上面的 Hearthian 备选）
- [ ] 打开飞船日志：map 模式能看到两颗星球节点、rumor 模式能看到 8 张卡片连线
- [ ] 无碰撞的废墟/塔只作视觉背景，玩家会穿过——若需要站上去，后续在 Unity 里补碰撞体

## 七、本轮改动文件清单

| 文件 | 改动 |
|------|------|
| `planets/singer_world.json` | details/scatter 全部换成验证过的基础游戏路径；删 mapMode.manualPosition |
| `planets/god_realm.json` | 录音石路径修正 + 加路标/火炬/容器布景；删 mapMode.manualPosition |
| `systems/outing_system.json` | 新增 entryPositions（8 个 Entry 的 rumor 模式坐标）|

---

# 5.26（三）场景丰富化 — 启用 DLC 资产，参考原版布景手法

> 进游戏后物品能正常出现，但场景过于简陋。本轮大幅丰富布景。用户已购买 DLC（Echoes of the Eye），因此本轮启用 Stranger/梦境世界资产。

## 一、布景设计思路（借鉴原版"分层布景"）

原版星球之所以耐看，靠的是**分层**：大型建筑（剪影）+ 中型结构（连接）+ 小型道具（叙事）+ 散布植被（铺底）+ 灯光（氛围）。本轮按这个思路重做世末之城：

| 层次 | 用的资产 | 作用 |
|------|---------|------|
| 大型建筑 | Stranger 木屋群（DLC）：两层木屋 / 神器木屋 / 梦火木屋 / 残破木屋 / 焚毁滑梯木屋 / 大型·超大型投影木屋 | 构成一座真正"有房子"的废城 |
| 竖向地标 | 挪麦重力炮 | 城市边缘高塔轮廓 |
| 古迹层 | 挪麦石质废墟 Ruin_Empty / Ruin_Campfire | 比木屋更古老的一层，叙事纵深 |
| 连接结构 | Owlk 桥段、发光挪麦门廊 | 引导动线 |
| 灯光 | 赫斯人蓝灯笼 ×4、挪麦立式火炬 ×2 | 昏暗多雨城市的关键氛围 |
| 道具 | 挪麦长桌 / 长椅 / 书架 | 广场与音乐厅的生活气息 |
| 植被散布 | 梦境湿地树 ×30、低矮灌木 ×12、枯灌木 ×20、陶器碎片 ×25 | 全星球铺底，消除荒凉 |

> **氛围契合**：Stranger（回声）的木质建筑 + 梦境世界的湿地树，本身就是"衰败、潮湿、孤寂"的美学，与《世末歌者》"困于鸥停之雨的废城"高度契合，比纯挪麦石建筑更贴主题。

## 二、关键修正：按球面半径校正每个 prop 的高度

之前 prop 的 `y` 值是随手填的（多为 290/300/301），但星球是半径约 301 的球面——离极点越远，地表的 y 越低。例如塔在 (x=90,z=55) 处地表 y≈282，却被放在 y=300，**悬空约 18 米**。这也是"显得简陋"的一个原因。

本轮对每个 detail 用 `y ≈ √(301² − x² − z²)` 重新计算贴地高度（神谕之境用半径 101 同理）。信号源、对话体、揭示体、占位角色的高度也一并对齐。

> 后续仍建议进游戏用 Debug Raycast（按 P）做最后微调——大型建筑的 pivot 不一定在底部。

## 三、神谕之境同步丰富

在赌约石碑区加了：第二个路标、挪麦长椅、**紫色裂纹重力结晶 ×3**（装饰用，不产生引力场）、**结晶巨岩**，呼应"白紫色结晶星球"的设定。

## 四、资产清单（全部来自社区验证表）

- DLC（`RingWorld_Body` / `DreamWorld_Body`）：Stranger 木屋 ×8、Owlk 桥、梦境树
- 基础游戏：挪麦重力炮、十字路口废墟、发光门廊、立式火炬、长桌/长椅/书架、蓝灯笼、陶器碎片、灌木、裂纹结晶、结晶巨岩、索拉努姆占位

## 五、验证

- `dotnet build`：0 警告 0 错误。
- 世末之城 25 个 details + 4 类散布；神谕之境 11 个 details。JSON 校验通过。

## 六、进游戏微调提示

- ⚠ Stranger 投影木屋有"背面剔除"（从外面能看穿后墙），已尽量用实心的两层木屋/神器木屋做近景，投影木屋只作远景天际线。
- 大型建筑 pivot 可能不在底部，若有悬空/陷地用按 P 取坐标微调 `scale`/`y`。
- 散布是全星球随机分布，城区密度靠 details 保证；若想城外也更密可调高 scatter 的 count。
- 木屋偏大，如某栋过大可加 `"scale": 0.7~0.9`。

---

# 5.26（四）挪麦文字修复 + 地表材质（HeightMap）

> 进游戏反馈：① 挪麦文字无法翻译、且凭空漂浮（未依附墙面）；② 星球表面是一颗光秃秃的白球，无材质、无起伏，世末之城显得大而空旷。

## 一、挪麦文字：改用 `cairn`(石碑) 类型

查 NH 的 `TranslatorTextInfo` schema 发现两个问题：

1. **`alignRadial` 不是 translatorText 的合法字段**（它只属于 `details`）。我之前给文字写了 `alignRadial`，根本没生效，导致朝向错乱、文字漂浮。translatorText 的朝向应由 **`normal`**（文字面朝的方向）控制。
2. `type:"wall"` 的螺旋文字本身不带墙体几何——它需要贴在一面真实的墙上，否则就是一组悬空的弧线，且不好对准翻译。

**修复**：把歌者日记和赌约石碑都改成 **`type:"cairn"`**——挪麦石碑是**自带石质几何体、立在地面、必定可被翻译器对准**的物件，正好对应"石碑"的设定，从根本上消除漂浮与无法翻译的问题。同时用 `normal`（径向朝上）让石碑立直，去掉了非法的 `alignRadial` / `seed`。

```json
"translatorText": [
    {
        "xmlFile": "planets/text/bet_stele.xml",
        "type": "cairn",
        "position": { "x": 0, "y": 101, "z": 0 },
        "normal": { "x": 0, "y": 1, "z": 0 }
    }
]
```

> 若后续仍想要"墙上的螺旋文字"的原版观感，需要：放一面挪麦白板/墙体 prop，再用 `type:"wall"` + 正确的 `normal`/`parentPath` 贴上去——这一步需要进游戏反复对位，故本轮先用必定可用的 cairn。

## 二、地表材质：HeightMap + 程序化贴图

白球的根因是星球没有 `HeightMap`（地表颜色贴图 + 高度贴图）。本轮用 numpy 程序化生成了等距柱状投影贴图：

| 文件 | 内容 |
|------|------|
| `planets/textures/singer_ground.png` | 世末之城·阴湿青灰废墟色（分形噪声 + 积水暗斑）|
| `planets/textures/singer_height.png` | 世末之城·轻微起伏高度图 |
| `planets/textures/god_ground.png` | 神谕之境·淡紫白结晶色 |
| `planets/textures/god_height.png` | 神谕之境·轻微起伏高度图 |

生成脚本在 `TheSingerOfTheEnd/gen_textures.py`（已移出 `planets/`，不打包进 MOD，可随时改色重跑）。

星球配置加 `HeightMap`：

```json
"HeightMap": {
    "textureMap": "planets/textures/singer_ground.png",
    "heightMap": "planets/textures/singer_height.png",
    "minHeight": 300, "maxHeight": 302, "smoothness": 0.1
}
```

**关键设计**：起伏区间设得很窄（半径 300–302，仅 ±1）。因为之前所有 prop 都是按"到球心距离 ≈ 301"摆放的，把地形也压在 301 附近，**所有建筑/道具就自动贴合新地表，不用再挪一遍**。贴图提供颜色/材质，轻微起伏打破"完美光球"的呆板感。

配套微调：
- `Base.groundSize` 降到 298 / 98（低于地形最低处），作为隐藏的兜底球面，避免与 HeightMap 地表叠加穿模。
- 出生点抬到 y=305 / 飞船 306（高于地形峰值 302），落地不会卡进地形。

## 三、关于"体积大、空旷"

本轮**没有缩小星球**——因为半径一改，几十个 prop 的坐标就得全部重算，风险高。改为用**地表材质 + 起伏 + 浓雾**来解决"空"的观感：世末之城 `fogDensity 0.55`，可视距离本就有限，玩家看不到整颗空球；加上地表纹理和城区密集的建筑，空旷感会明显改善。
若你看过实际效果后仍觉得太大，告诉我，我再做一次"缩小 + 整体重摆"的协调改动。

## 四、验证

- `dotnet build`：0 警告 0 错误；4 张贴图已复制到 MOD 的 `planets/textures/`。
- 两个 planet JSON 通过语法校验。

## 五、本轮改动文件

| 文件 | 改动 |
|------|------|
| `planets/textures/*.png` | **新增** 4 张程序化地表贴图 |
| `gen_textures.py` | **新增** 贴图生成脚本（项目根，不打包）|
| `planets/singer_world.json` | 加 HeightMap；日记 wall→cairn（修 normal/去 alignRadial）；groundSize 298；出生点抬高 |
| `planets/god_realm.json` | 加 HeightMap；石碑 wall→cairn；groundSize 98 |

---

# 5.26（五）仿木炉星重建地形 + 飞船日志缩略图

> 目标：① 删除杂乱的 Stranger 木屋建筑，改用木炉星风格的自然地貌（丘陵+散布植被）作为世末之城底色，把故事道具集中在北极平整空地；② 生成飞船日志"no photo"缺失的 8 张缩略图。

## 一、地形重建：仿木炉星构造

### 1.1 设计思路

| 层次 | 实现 |
|------|------|
| **地形起伏** | `HeightMap` 重新生成：北极（lat ≥ 70°）为平整空地（radius ≈ 301，放故事道具），外圈丘陵山谷（291–313），海拔高度按噪声分形 |
| **地表色彩** | 随高度分层：低地苔绿/积水暗 → 中地灰绿 → 高地岩灰 → 峰顶浅灰，呼应木炉星湿润青绿调色 |
| **植被散布** | `scatter` 4 类（不固定位置，自动跟随地形）：木炉星红杉 ×28、梦境湿地树 ×18、矮灌木丛 ×30、枯灌木 ×12 |
| **陶器点缀** | StatueIsland 陶器碎片 ×15（废墟感） |
| **故事道具** | `details` 仅保留叙事必须的 17 个（高塔、音乐厅废墟、发光门廊、灯笼、火炬、桌椅书架、天依/歌者占位）全部集中在北极空地 |

**关键参数改动**：

- `HeightMap.minHeight = 291, maxHeight = 313, smoothness = 0.1`（起伏更大，地貌更真实）
- `Base.groundSize = 285`（进一步低于地形最低处 291，避免双层穿模）
- 所有故事道具 `y ≈ √(301² − x² − z²)`，置于平整北极带

### 1.2 贴图重新生成

`gen_textures.py` 更新了北极平地逻辑：

```python
V_FLAT = 0.4545  # 对应 minHeight=291, range=22 → radius ≈ 301
flatness = smoothstep(50, 70, lat2d)   # 北极(lat≥70)全平, 70→50 渐变
height_s = flatness * V_FLAT + (1 - flatness) * terrain
```

4 张贴图全部重新生成，真实反映丘陵地形。

### 1.3 场景组成

删除之前（Round 3）引入的 Stranger 木屋群（pivot 难对位，丘陵地形上容易悬空），换回以自然植被为主、故事建筑为辅的布局：

| 元素 | 内容 |
|------|------|
| 大型地标 | 挪麦重力炮（高塔）|
| 音乐厅区 | BH `Ruin_Empty` 废墟后壁 + 书架 + 火炬 |
| 广场区 | `Ruin_Campfire` + 发光门廊 + 蓝灯笼 + 桌椅 |
| 植被底色 | TH 红杉 ×28 + DW 湿地树 ×18 + 矮灌木 ×30 + 枯灌木 ×12 全球散布 |
| 角色占位 | 天依(20,300,10) + 歌者(0,296,-56)，均为 Solanum 仰望姿态 |

## 二、飞船日志缩略图（`gen_shiplog_sprites.py`）

**问题**：8 个 Entry 无图片（显示"no photo"黑块）。

**修复**：新建 `gen_shiplog_sprites.py`，用 PIL `ImageDraw` 生成 8 张 320×320 氛围缩略图（文件名 = Entry ID，NH `spriteFolder` 机制要求）。

| Entry ID | 缩略图主题 |
|----------|-----------|
| `SINGER_CITY` | 雨中城市天际线 + 月亮 |
| `CITY_SQUARE` | 孤独人影 + 水洼倒影 |
| `CITY_HALL` | 废弃音乐厅拱门暖光 |
| `CITY_TOWER` | 高塔穿云 + 蓝色塔顶光 |
| `GOD_REALM` | 紫白星球 + 结晶 |
| `BET_STELE` | 三块石碑（赌约三条规则）|
| `GOD_VOICE` | 发光结晶 + 声波弧线 |
| `THIRD_PARTY` | 注视之眼 |

两个 planet JSON 同步加 `"spriteFolder": "planets/shiplog/sprites"`。

## 三、验证

- `python gen_textures.py` → 生成 4 张贴图 ✅
- `python gen_shiplog_sprites.py` → 生成 8 张缩略图 ✅
- `dotnet build`：**0 警告 0 错误**，DLL 自动部署到 OWML ✅

## 四、本轮改动文件

| 文件 | 改动 |
|------|------|
| `planets/singer_world.json` | scatter 重做（植被化）；details 删 Stranger 木屋，保留 17 个叙事道具；HeightMap 参数更新；ShipLog 加 `spriteFolder` |
| `planets/god_realm.json` | ShipLog 加 `spriteFolder` |
| `planets/textures/singer_*.png` | 重新生成（丘陵地形）|
| `gen_textures.py` | 更新北极平地逻辑 |
| `planets/shiplog/sprites/*.png` | **新增** 8 张 320×320 缩略图 |
| `gen_shiplog_sprites.py` | **新增** 缩略图生成脚本（项目根，不打包）|

## 五、进游戏验证清单

- [ ] 地表纹理正确加载（青灰色带起伏，不再是白球）
- [ ] 北极平地区域植被+道具布局自然（散布植被自动跟地形）
- [ ] 飞船日志 8 个条目均显示对应缩略图（而非黑色 no photo）
- [ ] 高塔/废墟高度贴地（y ≈ 288–301 范围内）
- [ ] 天依 (20,300,10) / 歌者 (0,296,-56) 占位可见

---

# 5.26（七）NPC 行为、时间线管理器与新设置

> 目标：补全 `NpcBehavior.cs` 和 `TimelineManager.cs`，接入主函数；新增两条 MOD 设置；整理文档。

## 一、NpcBehavior.cs（从空文件实现）

### 职责

在场景内找到天依和歌者两个 NPC GameObject，为其添加面向玩家的转向逻辑和角色特定行为。

### 实现要点

| 行为 | 天依（蓝发女孩） | 歌者（阿绫） |
|------|----------------|-------------|
| 默认 | 仰望天空（不转向，维持 Solanum 动画姿态）| 始终微幅上下摆动（演唱手势，sin 波 1.1 Hz）|
| 玩家靠近（< 14 m）| 转向玩家 | 转向玩家 |
| `AMPLIFIER_REPAIRED` 为真 | 转向音乐厅方向（`HallLocalPos`） | 同上 |

**关键算法**：`RotateToward()` 先用 `Vector3.ProjectOnPlane(dir, transform.up)` 把目标方向投影到星球表面切平面，再 `Quaternion.RotateTowards` 以 60°/s 平滑旋转。这样在球形星球上旋转时不会侧倾。

**Animator 兼容**：`Start()` 中取 `GetComponentInChildren<Animator>()` 并令 `applyRootMotion = false`，防止 Animator 覆盖我们对 pivot 的旋转。

**挂载方式**：`NpcBehavior.Setup(INewHorizons nh)` 静态方法，用 `FindDeep(planet.transform, npcName)` 深度搜索星球层级中的目标对象，找到后 `AddComponent<NpcBehavior>()`；找不到则静默跳过（兼容歌者 prefab 尚未打包的情况）。

### 在主函数中的挂载点

```csharp
// SetupGraphics() 协程末尾，玩家身体就绪后：
NpcBehavior.Setup(NewHorizons);
```

---

## 二、TimelineManager.cs（从空文件实现）

### 职责

跟踪 OW 22 分钟游戏循环的剩余时间，驱动 God Ray 强度和雨量随时间渐变；`AMPLIFIER_REPAIRED` 触发后由 `EndingJudge` 调用 `PlayTrueEnd()`，播放"雨停 → 光束爆发 → 平静"三段演出。

### 正常时间线（非 True End）

| 时间进度 t | God Ray 强度 | 雨量（粒/秒） |
|-----------|-------------|------------|
| 0（循环开始）| 0.5 | 4000 |
| 0.5（11 min）| 0.675 | 4000（不变）|
| 1（超新星前）| 0.85 | 6000 |

t = `(1 − 剩余秒数 / 1320)` ，用 `TimeLoop.GetSecondsRemaining()` 读取。

### True End 三阶段演出

| 阶段 | 时长 | 效果 |
|------|------|------|
| 雨停 | 0–3 s | 雨量从 6000 → 0（线性） |
| 光束爆发 | 3–8 s | God Ray 强度 0.85 → 1.5 |
| 平静 | 8–13 s | God Ray 强度 1.5 → 1.0 |

`PlayTrueEnd()` 由 `EndingJudge.TriggerTrueEnding()` 调用（已在 EndingJudge 中接入）。

### 设置同步

`Configure()` 调用 `TimelineManager.Instance?.OnSettingsChanged()` 更新 `_shadersEnabled` 缓存，避免每帧查 Config。

### 在主函数中的挂载点

```csharp
// SetupGraphics() 协程末尾，与 NpcBehavior.Setup 同步：
TimelineManager.Setup();
```

---

## 三、RainController.cs 新增接口

为支持 `TimelineManager` 在运行时控制雨量，新增：

```csharp
public static RainController Instance { get; private set; }  // 单例
public void SetEmissionRate(float rate)   // TimelineManager 调用
public void EnablePS()  / DisablePS()     // Shader 开关调用
```

---

## 四、新增 MOD 设置

`default-config.json` 追加两个设置项：

### 4.1 `Shader Effects Enabled (启用Shader效果)`（布尔，默认 true）

控制全部自定义 shader 效果的总开关：

- `false`：`SetupGraphics()` 跳过 `GodRayController` 和 `RainController` 的初始化；`Configure()` 立即 disable 已存在的控制器（`GodRayController.enabled = false`、`RainController.DisablePS()`）。
- 适用场景：性能受限设备、排查其它 MOD 冲突、截图时临时关闭。

### 4.2 `Reset Story Progress (重置剧情进度)`（布尔，默认 false）

一次性剧情重置触发器，用于测试时快速清空进度：

**触发流程**：
1. 玩家在 OWML Mod Manager 中将此项设为 `true`，保存。
2. `Configure()` 检测到 `true`，调用 `ResetStoryProgress()`：
   - `DialogueConditionManager.SharedInstance.SetConditionState(key, false)` 清除所有对话条件
   - `PlayerData.SetPersistentCondition(key, false)` 清除持久条件（含 `AMPLIFIER_EVER_REPAIRED`）
   - 清除的 key：`TALKED_TO_TIANYI`、`AMPLIFIER_REPAIRED`、`TIANYI_HEARD_SONG`、`AMPLIFIER_EVER_REPAIRED`
3. 立即调用 `config.SetSettingsValue("Reset Story Progress...", false)` 将开关复原为 false，避免下次启动重复触发。
4. 控制台打印重置成功日志。

> ⚠ 飞船日志已揭示的 Fact（如 `TRUE_ENDING_FACT`、`AMPLIFIER_FIXED`）无法通过此方式清除（OW 不暴露公开的 UnRevealFact API）。若需完整重置飞船日志，在游戏内"选项 → 重置游戏存档"或删除存档文件。

---

## 五、主函数改动汇总（TheSingerOfTheEnd.cs）

| 位置 | 改动 |
|------|------|
| `Awake/Start` | 读取并缓存 `ShadersEnabled` 属性（`public bool ShadersEnabled { get; private set; }`）|
| `Configure()` | 更新 `ShadersEnabled`；同步 `GodRayController.enabled` + `RainController.EnablePS/DisablePS`；处理重置触发；调用 `TimelineManager.OnSettingsChanged()` |
| `SetupGraphics()` | `if (ShadersEnabled)` 分支保护 GodRay + Rain 初始化；末尾调 `NpcBehavior.Setup` + `TimelineManager.Setup` |
| `ResetStoryProgress()` | 新增私有方法，清空条件后 `config.SetSettingsValue` 复原开关 |

---

## 六、验证

```
dotnet build → 0 警告 0 错误
```

## 七、进游戏验证清单

- [ ] 进入鸥停星系，控制台输出 `SetupGraphics complete`
- [ ] 靠近天依（距离 < 14 m），她从仰望天空变为转向玩家
- [ ] 靠近歌者，她转向玩家，同时有微幅上下摆动
- [ ] 修复扩音装置后（`AMPLIFIER_REPAIRED=true`），天依转向音乐厅方向
- [ ] God Ray 强度随游戏时间推移逐渐增强（22 min 内可观察到变化）
- [ ] 修复扩音装置后，雨粒子在 3 秒内逐渐停止，God Ray 在 5 秒内爆发到最亮，再缓降
- [ ] OWML 设置中关闭"启用Shader效果"，重载场景后无雨粒子、无 God Rays 光束
- [ ] OWML 设置中开启"重置剧情进度"，保存后控制台输出重置成功，设置自动恢复为 false

## 八、本轮改动文件

| 文件 | 改动 |
|------|------|
| `NpcBehavior.cs` | **从空文件实现**：面向玩家转向、天依剧情状态联动、歌者摆动动画 |
| `TimelineManager.cs` | **从空文件实现**：循环时间线驱动 + True End 三段演出 |
| `RainController.cs` | 新增 `Instance`、`SetEmissionRate()`、`EnablePS()`、`DisablePS()` |
| `EndingJudge.cs` | `TriggerTrueEnding()` 新增调用 `TimelineManager.Instance?.PlayTrueEnd()` |
| `TheSingerOfTheEnd.cs` | 完整重写：新增 `ShadersEnabled` 属性、`Configure()` 扩展、`ResetStoryProgress()`、`SetupGraphics()` 接入 NpcBehavior + TimelineManager |
| `default-config.json` | 新增 `Shader Effects Enabled` 和 `Reset Story Progress` 两条设置 |

---

# 5.26（六）自定义 Shader 接入游戏

> 目标：将已打包好的 AssetBundle（`assets/shaders/shaders`）里的三个自定义材质接入游戏，实现体积雨、地面涟漪水洼、屏幕空间 God Rays 三种图形效果。

## 一、资产管线验证（Stage A）

AssetBundle 已由用户在 Unity 中打包，位置：

```
TheSingerOfTheEnd/TheSingerOfTheEnd/assets/shaders/shaders   ← 无扩展名，AssetBundle 格式
```

Bundle 内包含三个材质：

| 材质路径（bundle 内） | Shader | 用途 |
|----------------------|--------|------|
| `Assets/Materials/GodRayMat.mat` | `Custom/GodRays` | 屏幕空间 God Rays 后处理 |
| `Assets/Materials/RainMat.mat` | `Custom/VolumetricRain` | 体积雨粒子渲染 |
| `Assets/Materials/RippleMat.mat` | `Custom/RainRipple` | 地面涟漪水洼 |

`.csproj` 已有 `<None Include="assets\**\*.*">` glob，编译时自动将 bundle 复制到 MOD 输出目录。

## 二、新增 C# 模块

### 2.1 `AssetLoader.cs`（新建）

单例式静态加载器，在 MOD `Start()` 阶段调用 `Preload()`，只加载一次（用 `_tried` bool 防二次 Load 报错）：

```csharp
var path = Path.Combine(ModHelper.Manifest.ModFolderPath, "assets/shaders/shaders");
_bundle = AssetBundle.LoadFromFile(path);
GodRay  = _bundle.LoadAsset<Material>("Assets/Materials/GodRayMat.mat");
Rain    = _bundle.LoadAsset<Material>("Assets/Materials/RainMat.mat");
Ripple  = _bundle.LoadAsset<Material>("Assets/Materials/RippleMat.mat");
```

暴露 `AssetLoader.GodRay / .Rain / .Ripple` 三个静态 Material 属性供其余模块直接引用。

### 2.2 `GodRayController.cs`（新建）

`[RequireComponent(typeof(Camera))]`，挂载到玩家主相机 GameObject，利用 `OnRenderImage` 实现三 Pass 光束渲染：

| Pass | 作用 |
|------|------|
| 0 — Occlusion | 采样 `_CameraDepthTexture`，用 `step(_DepthThreshold, Linear01Depth)` 把天空（depth≈1）提取为白色遮罩，前景（星球/建筑）保持黑色，从而只让天空区域散射 |
| 1 — RadialBlur | 从屏幕光源位置 `_LightPos` 向外做径向步进采样（64 步），叠加 `illuminationDecay *= _Decay` 衰减，模拟光子被遮挡后向外扩散的丁达尔效应；重复一次使光束更长 |
| 2 — Composite | 将原场景纹理 `_SceneTex` 与光束图 相加混合，`_Intensity` 控制强度 |

关键保护：当太阳在相机背后（`WorldToViewportPoint.z ≤ 0`）时直接 `Blit` 跳过，避免反向光束 artifact。

`Intensity` 设为 public，供结局演出（True End 雨停穿云）动态拉满。

### 2.3 `RainController.cs`（新建）

静态 `Setup()` 创建跟随玩家的 `ParticleSystem`，解决球面星球重力方向不一致的问题：

```
simulationSpace = Local          ← 在玩家本地坐标系模拟
velocityOverLifetime.y = -30     ← Local 空间"向下"= 永远指向脚底
gravityModifier = 0              ← 不用世界重力(球面朝向不同)
```

Box 发射器置于玩家头顶 15 m，宽 40×40 m，4000 粒/秒，最大 8000 粒，Billboard 渲染使用 `RainMat`。

`Update()` 每帧判断玩家与"世末之城"中心的距离，超过 520 m（大气/雾层边界）时关闭 emission，避免在太空或神谕之境出现降雨。

`SpawnPuddles()` 在北极平整空地放置 3 个 `Quad`，使用 `RippleMat` 材质，父级设为星球 transform（随星球旋转/公转），`Euler(90,0,0)` 平铺朝上。

### 2.4 `TheSingerOfTheEnd.cs`（修改）

`Start()` 中新增：

```csharp
AssetLoader.Preload();   // 加载 bundle，缓存三个材质
```

`OnStarSystemLoaded` 检测到 `outing_system` 后新增：

```csharp
StartCoroutine(SetupGraphics());
```

`SetupGraphics()` 协程轮询 `Locator.GetPlayerCamera()`（30 s 超时），相机就绪后：
1. 在相机 GameObject 上 `AddComponent<GodRayController>().Init(GodRayMat, sunTransform)`
2. 继续等 `Locator.GetPlayerTransform()` 就绪后调 `RainController.Setup()`

## 三、各 Shader 效果说明

### 3.1 God Rays（`GodRay.shader`，三 Pass）

**视觉效果**：从鸥停之星（橙红恒星）向外放射的丁达尔光束，穿透云层/大气边界。光束强度随距离衰减，仅在天空（无建筑遮挡）区域显现。

**实现原理**：屏幕空间径向模糊光散射（Crepuscular Rays），通过相机深度纹理提取天空遮罩，再从光源视口坐标向外做径向积分，最终叠加到场景上。

**关键参数**：`_Density`（光束密度）、`_Decay`（衰减速率）、`_Exposure`（曝光）、`_DepthThreshold`（天/地分割深度阈值）。

### 3.2 体积雨（`VolumetricRain.shader`，单 Pass）

**视觉效果**：细长雨丝粒子，从视角上方笔直下落，带 Fresnel 边缘高亮，呈半透明玻璃质感，下落速度均匀（局部坐标 -Y 方向）。

**实现原理**：Billboard 粒子 + 视空间拉伸。在顶点着色器中用 `UnityObjectToViewPos` 求粒子中心视空间坐标，在片段着色器中计算到轴线的距离（`distToAxis`）做高斯截面，上下端软渐变（`headTail`），加 Fresnel（`dot(viewDir, N)`）模拟雨丝边缘折射。

**关键参数**：`_StretchFactor`（纵向拉伸比）、`_Width`（雨丝粗细）、`_FresnelPower`（Fresnel 指数）、`_Color`。

### 3.3 涟漪水洼（`RainRipple.shader`，单 Pass）

**视觉效果**：地面 Quad 上同心圆环向外扩散，多组涟漪叠加，形成"雨打水面"的波纹动画，带镜面反射高光和 Fresnel 边缘。

**实现原理**：程序化高度场 + 法线扰动 + 光照。用 `sin(d × scale − time × speed) × exp(−d × falloff)` 生成单圈涟漪，4 组不同中心叠加为高度场 `h`；用 `ddx(h)` / `ddy(h)` 求屏幕空间偏导数，扰动表面法线；最后 Blinn-Phong 高光 + Fresnel 混合水色与反射色。

**关键参数**：`_RippleScale`（波纹空间频率）、`_RippleSpeed`（扩散速度）、`_RippleFalloff`（衰减半径）、`_RippleStrength`（法线扰动强度）、`_Shininess`。

## 四、构建与部署验证

```
dotnet build → 0 警告 0 错误
```

- `AssetLoader.cs` / `GodRayController.cs` / `RainController.cs` 全部编译进 DLL。
- `assets/shaders/shaders` bundle 随 `<None Include>` glob 复制到输出目录。
- DLL 自动部署到 OWML mods 文件夹。

## 五、进游戏验证清单

- [ ] 控制台输出 `shaders bundle 加载成功`（AssetLoader 加载 bundle 成功）
- [ ] 控制台输出 `材质加载: GodRay=True, Rain=True, Ripple=True`
- [ ] 控制台输出 `GodRayController attached` / `降雨已部署`
- [ ] 面朝鸥停之星时可见橙红色放射光束（God Rays）；背向恒星时光束消失
- [ ] 在世末之城附近（距星球中心 < 520 m）有雨丝粒子从头顶落下
- [ ] 城市区域外（太空中/神谕之境）无降雨粒子
- [ ] 北极空地 3 处地面水洼显示同心涟漪动画
- [ ] True End 触发后可在代码里将 `GodRayController.Intensity` 拉高，光束更强烈

## 六、本轮改动文件

| 文件 | 改动 |
|------|------|
| `TheSingerOfTheEnd.cs` | 加 `AssetLoader.Preload()` 调用；加 `SetupGraphics()` 协程；`using System.Collections` |
| `AssetLoader.cs` | **新建**：bundle 加载与材质缓存 |
| `GodRayController.cs` | **新建**：屏幕空间 God Rays 后处理（三 Pass OnRenderImage）|
| `RainController.cs` | **新建**：体积雨粒子系统 + 涟漪水洼部署 |
| `assets/shaders/GodRay.shader` | **已打包进 bundle**（三 Pass：Occlusion / RadialBlur / Composite）|
| `assets/shaders/VolumetricRain.shader` | **已打包进 bundle**（Billboard 雨丝，视空间拉伸）|
| `assets/shaders/RainRipple.shader` | **已打包进 bundle**（程序化涟漪，ddx/ddy 法线扰动）|
